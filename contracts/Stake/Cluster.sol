pragma solidity >=0.4.21 <0.7.0;

contract Cluster {

    uint256 public undelegationWaitTime;
    function isClusterValid(address _cluster) public returns(bool) {

    }

    function delegate(address _delegator, address _cluster, uint256 _amount) public {

    }

    function undelegate(address _delegator, address _cluster, uint256 _amount) public {

    }
}